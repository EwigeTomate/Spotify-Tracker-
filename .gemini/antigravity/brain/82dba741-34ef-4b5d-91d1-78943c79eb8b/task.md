# Tasks: Multi-User, Detailed Logging, Gemini AI & Wrapped

- [x] Database Schema Refactoring & Migrations
  - [x] Implement `users` table and migration for existing users in `db.js`
  - [x] Adjust `play_history` table primary keys and run migration in `db.js`
  - [x] Create `detailed_playback_logs` table in `db.js`
  - [x] Create `ai_reports` table in `db.js`
  - [x] Update all DB queries to filter by `spotify_user_id` and add user/log CRUD helpers
- [x] Refactoring Spotify Integration (`spotify.js`)
  - [x] Modify token operations to use the new `users` database table by `spotify_user_id`
  - [x] Add `getUserProfile` parsing and `getEpisode` integrations
- [x] Background Logging & Synchronization (`collector.js`)
  - [x] Loop history collection over all database users
  - [x] Implement the active playback poller running every 30 seconds to log volume, device, and progress snapshots
- [x] API Endpoint Enhancements (`server.js`)
  - [x] Implement middleware to extract the active user ID
  - [x] Update OAuth callback to save users and run database historical migration from `'default_user'`
  - [x] Create `/api/users`, `/api/reports/generate`, `/api/reports`, and `/api/export` endpoints
- [x] Frontend HTML Structure (`index.html`)
  - [x] Add User Switcher dropdown to the sticky top bar
  - [x] Add `AI-Analysen` sidebar navigation item and `#ai-reports-view` view container
  - [x] Add full-screen `#wrapped-view` overlay structure with slides and progress bars
- [x] Frontend Styles (`style.css`)
  - [x] Add styling for User Switcher dropdown
  - [x] Add styling for AI-Reports cards and markdown sheets
  - [x] Add full-screen animated slide deck styles for Spotify Wrapped
- [x] Frontend Application Logic (`app.js`)
  - [x] Handle switcher events, user switching, and active user initialization via localStorage
  - [x] Add export downloader logic
  - [x] Implement Gemini key input and daily summary/wrapped generator triggers
  - [x] Implement the interactive Wrapped story slider deck (keyboard controls, progress bars, animations)
- [/] Verification & Polish
  - [ ] Verify multi-user logins and session persistence
  - [ ] Verify detailed logging and export file contents
  - [ ] Verify Gemini integration and Spotify Wrapped slides
